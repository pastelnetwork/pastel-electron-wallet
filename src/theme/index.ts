import { DefaultTheme } from 'styled-components'

import { colors as ThemeColors } from './colors'
import { fonts as ThemeFonts } from './fonts'

export const theme: DefaultTheme = {
  colors: ThemeColors,
  fonts: ThemeFonts,
}
